---
title: "Shipping hermes-agent"
date: 2026-03-19
mood: focused
tags: [daily, git, coding]
type: daily
---

Thursday. 13 commits landed across 1 repo. Most of the motion clustered in hermes-agent.

## What shipped

### hermes-agent

- 13 commits
- feat: show reasoning/thinking blocks when show_reasoning is enabled; fix: harden sentinel guard for /stop during setup and shutdown; fix(cli): expand session list columns for full ID visibility; docs: align venv path to match installer (venv/ not .venv/); fix: /model command — bare provider names, custom endpoint display; fix(codex): ensure reasoning items have required following item in API input; refactor: use SessionDB instead of JSON files for ACP session persistence; fix: persist ACP sessions to disk so they survive process restarts; fix(codex): treat reasoning-only responses as incomplete, not stop; fix(delegate): move _saved_tool_names assignment to correct scope; fix: revert accidental summary_target_tokens change + add docs for context_length config; fix: auto-detect local model name and context length for local servers; fix: detect context length for custom model endpoints via fuzzy matching + config override
- Touched: cli.py, run_agent.py

## Commit log

- 22:44 `hermes-agent`: feat: show reasoning/thinking blocks when show_reasoning is enabled
- 21:26 `hermes-agent`: fix: harden sentinel guard for /stop during setup and shutdown
- 21:17 `hermes-agent`: fix(cli): expand session list columns for full ID visibility
- 21:16 `hermes-agent`: docs: align venv path to match installer (venv/ not .venv/)
- 14:20 `hermes-agent`: fix: /model command — bare provider names, custom endpoint display
- 13:22 `hermes-agent`: fix(codex): ensure reasoning items have required following item in API input
- 13:16 `hermes-agent`: refactor: use SessionDB instead of JSON files for ACP session persistence
- 13:03 `hermes-agent`: fix: persist ACP sessions to disk so they survive process restarts
- 13:00 `hermes-agent`: fix(codex): treat reasoning-only responses as incomplete, not stop
- 12:25 `hermes-agent`: fix(delegate): move _saved_tool_names assignment to correct scope
- 09:00 `hermes-agent`: fix: revert accidental summary_target_tokens change + add docs for context_length config
- 08:57 `hermes-agent`: fix: auto-detect local model name and context length for local servers
- 08:32 `hermes-agent`: fix: detect context length for custom model endpoints via fuzzy matching + config override
